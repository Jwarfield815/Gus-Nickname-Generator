Home Page button does not work when running locally, directs to `/nickname`
Run `python -m http.server` in root of the project and navigate to `localhost:8000` to view website<br>
Alternatively, go to `https://portfolio.goeshardinc.com/gusnames` to view
